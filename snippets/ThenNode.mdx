export const ThenNode = ({ children }) => {
  return (
    <span
      style={{
        display: "inline-flex",
        height: "24px",
        minWidth: "24px",
        padding: "0px 6px",
        justifyContent: "center",
        alignItems: "center",
        gap: "4px",
        borderRadius: "0px 6px 6px 0px",
        border: "0.5px solid #F4CDB6",
        background: "#FFEFE6",
        color: "#FF781B",
        fontFamily: "Geist, sans-serif",
        fontSize: "13px",
        fontStyle: "normal",
        fontWeight: 400,
        lineHeight: "18px",
        verticalAlign: "middle",
      }}
    >
      THEN
    </span>
  );
} 