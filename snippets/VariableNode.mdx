export const VariableNode = ({ children }) => {
  return (
    <span
      style={{
        display: "inline-flex",
        height: 24,
        padding: "0px 6px",
        justifyContent: "center",
        alignItems: "center",
        gap: 4,
        borderRadius: 6,
        background: "rgba(19, 124, 249, 0.12)",
        color: "var(--label-label-link, #007EFF)",
        fontFamily: "Geist, sans-serif",
        fontSize: 13,
        fontStyle: "normal",
        fontWeight: 400,
        lineHeight: "18px",
      }}
    >
      {children}
    </span>
  );
}