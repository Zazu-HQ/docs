export const Comparison = ({ children }) => {
  return (
    <span
      style={{
        display: "inline-flex",
        height: "24px",
        minWidth: "24px",
        padding: "0px 6px",
        justifyContent: "center",
        alignItems: "center",
        gap: "4px",
        background: "var(--background-bg-dim, #F1F1F1)",
        color: "var(--label-label-base, #292929)",
        fontFamily: "Geist, sans-serif",
        fontSize: "13px",
        fontStyle: "normal",
        fontWeight: 400,
        lineHeight: "18px",
        verticalAlign: "middle",
      }}
    >
      {children}
    </span>
  );
} 